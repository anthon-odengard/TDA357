Student(_idnr_, sname, login, progname, branch)
	program  →  Program.name
	login unique
	
Course(_code_, cname, depname, credits, classification, capacity, reqcourse)
	depname → Department.depname
	
Taken(idnr, _code_, grade)
	idnr → Student.idnr
	code →  Course.code

Waitinglist(_idnr_, _code_, position)
	idnr → Student.idnr
	code →  Course.code
	(code, position) unique

Department(_depname_, depabbr)
	abbreviation unique

Program(_progname_, progabbr)
	
	

